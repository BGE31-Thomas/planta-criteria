{% extends 'base.html.twig' %}

{% block title %}Ajouter / modifier un critère{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Ajouter / modifier un critère  </h1>

    <input type="text" id="description" placeholder="Description du critère...">
    <input type="text" id="organe" placeholder="Organe du critère...">

    <button id="submit">Ajouter / modifier le critère</button>

</div>

<script>
document.getElementById('submit').addEventListener('click', async function() {  
  
    let description = document.getElementById('description').value;
    let organe = document.getElementById('organe').value;

    if (nom.length < 2) {
        alert('Le nom doit comporter au moins 2 caractères.');
        return;
    }



    let res = await fetch('/critere/add', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ description, organe })
    });

    if (res.ok) {
        alert('Critère ajouté / modifié avec succès !');
        // Optionnel : rediriger ou réinitialiser le formulaire
    } else {
        alert('Erreur lors de l\'ajout / modification du critère.');
    }
});
{% endblock %}
